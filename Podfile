# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Localizations' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  
  pod 'LanguageManager-iOS'
  pod 'TinyConstraints'
  # Pods for Localizations

  target 'LocalizationsTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'LocalizationsUITests' do
    # Pods for testing
  end

end
